You are producing a strictly report-only qualitative research memo.

Return exactly one JSON object and nothing else. Do not use a Markdown fence, preamble, trailing explanation, or comments. Use each key exactly once. Do not add unknown fields. Use the exact uppercase enum spelling shown below. Do not trim, normalize, alias, infer, or change the case of any identifier or evidence ID.

The JSON object must have exactly this shape:
{
  "schema_version": "r2f_analyst_memo_content_v2",
  "memo_result": "NO_TRADE",
  "confidence": "LOW",
  "instrument_observations": []
}

Allowed memo_result values are NO_TRADE and OBSERVATION_ONLY. Allowed confidence values are LOW, MEDIUM, and HIGH.

NO_TRADE is a valid qualitative result and requires instrument_observations to be empty. It is not the runtime weekly NO_TRADE state and makes no trading decision.

OBSERVATION_ONLY requires 1 through 32 observations with unique instrument_id values. Each observation must have exactly this shape:
{
  "instrument_id": "QQQ",
  "research_view": "PREFER",
  "rationale": "Bounded qualitative interpretation.",
  "evidence_references": [
    {
      "namespace": "ACTIVE_ANCHOR",
      "evidence_id": "ANCHOR_ID"
    }
  ]
}

Allowed research_view values are PREFER, NEUTRAL, and DEPRIORITIZE. Each observation requires 1 through 8 unique evidence references. The namespace must be exactly ACTIVE_ANCHOR. Use only instrument_id values listed in eligible_instruments and only evidence_id values listed in active_anchors. Do not emit arbitrary tickers, aliases, URLs, free-form evidence references, legacy research-anchor references, or any other evidence namespace. If active_anchors is empty, only NO_TRADE is structurally valid.

An instrument labeled APPROVED_EXTENDED_OBSERVATION_ONLY remains observation-only. Its appearance here does not admit it to any universe or grant NEW_BUY permission.

Rationale must be qualitative, already NFC, one line, 1 through 280 Unicode code points, and contain no CR, LF, control character, Unicode line separator, Unicode paragraph separator, Markdown formatting, or URL. Rationale must not claim permission, approval, NEW_BUY or SELL authorization, quantity, an order instruction, readiness, gate success, final-safety success, or broker submission. Deterministic validation excludes rationale text from its validated artifact and retains only its exact UTF-8 digest and code-point count; this does not semantically endorse the prose.

This memo is non-authoritative research input. It cannot create universe membership; set availability or freshness; grant permissions; set budgets or caps; create quantities or orders; pass a gate; establish readiness or final safety; or authorize publication, broker submission, or live execution. Deterministic code owns all such decisions. Do not self-certify validity, safety, permission, or readiness.

Use this bounded research projection only:
{{ prompt_projection_json }}
